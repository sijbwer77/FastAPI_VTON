# .env.example - Example environment variables for the FastAPI VTON project

# Database URL
# Example for SQLite: sqlite:///./resources/app.db
# Example for PostgreSQL: postgresql://user:password@host:port/dbname
DATABASE_URL="sqlite:///./resources/app.db"

# CORS Allowed Origins (comma-separated list of URLs)
# Use "*" for all origins during development (not recommended for production)
# Example: "http://localhost:3000,https://your-frontend-domain.com"
ALLOWED_ORIGINS="*,http://localhost:3000"

# JWT Secret Key
# A strong, random string used to sign JWTs.
# Generate one using: openssl rand -hex 32
SECRET_KEY="your-super-secret-jwt-key"

# Google OAuth Credentials
# Obtain these from Google Cloud Console (APIs & Services -> Credentials -> OAuth 2.0 Client IDs)
# Ensure "Authorized redirect URIs" includes your backend callback (e.g., http://localhost:8000/auth/google/callback)
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"
